spring:
  application:
    name: eureka-server
  profiles:
    active: server1

security:
  basic:
    enabled: true
  user:
    name: user
    password: pass

eureka:
  instance:
    lease-renewal-interval-in-seconds: 5
